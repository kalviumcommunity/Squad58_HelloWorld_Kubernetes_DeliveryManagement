# Backend

Express API for delivery menus and pricing simulation.

## Run

```bash
npm install
npm run dev
```

Backend listens on `http://localhost:8080`.

## Test

```bash
npm test
```

## Environment Variables

- `PORT` (default: `8080`)
- `PRICING_ALGO_VERSION` (`stable` or `canary`, default: `stable`)
